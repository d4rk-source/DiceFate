# Copy this to .env if you want to use the Makefile's deploy-local target manually.
# start-dev.sh does NOT need this file — it handles the key internally.

# Anvil account #0 (pre-funded, safe to commit for local dev only)
PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80
RPC_URL=http://127.0.0.1:8545

# Frontend (auto-written by start-dev.sh; only set manually for custom deployments)
NEXT_PUBLIC_RPC_URL=http://127.0.0.1:8545
